$(function () {
    $('#btn_sign_up').on('click', function (e) {
        e.preventDefault();
        $('#frm_sign_up').submit();
    });
    $('#frm_sign_up').validate({
        errorClass: 'error-msg-validate',
        rules: {
            username:{
                required: true,
                remote: {
                    url: $("#frm_sign_up").data('duplicateuser'),
                    type: "post",
                    data: {
                        username: function() {
                            return $('#frm_sign_up #username').val()
                        }
                    }
                }
            },
            email:{
                required: true,
                remote: {
                    url: $("#frm_sign_up").data('duplicateuser'),
                    type: "post",
                    data: {
                        email: function() {
                            return $('#frm_sign_up #email').val()
                        }
                    }
                },
                email:true
            },
            password:{
                required:true,
                minlength:6
            },
            confirm:{
                required:true,
                minlength:6,
                equalTo:"#password"
            }
        }
    });
    // $("#frm_sign_up").on('submit',function (e) {
    //     if($(this).valid()) {
    //         var data = $(this).serializeArray();
    //         var url = $(this).attr('action');
    //         $.post(url, data, function (resp) {
    //             if(resp.error==0){
    //                 window.location.href="http://projectqldsv.dev/admin/login";
    //             }
    //             if(resp.error==1)
    //             {
    //                 toastr.error(resp.message, 'Thông Báo!', {closeButton:true});
    //             }
    //         }, 'json');
    //         return false;
    //     }
    // });
});